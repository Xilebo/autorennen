function($) {
	$(document).ready( function() {
		var board = $(".board");
		board.click(function (eventHandler) {
			//~ var fieldX = eventHandler.offsetX / tileSizeX
			//~ var fieldY = eventHandler.offsetY / tileSizeY
		});
	});
}(jQuery);
